name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          name: Persian RTL Fixer ${{ github.ref_name }}
          body: |
            ## 🎉 Persian RTL Fixer for GitHub Copilot
            
            ### ✨ Features | ویژگی‌ها
            
            - 🔄 Auto-detect Persian/Arabic and English text direction
            - 🎨 Custom font support for Persian and English
            - ⚡ Optimized performance with debouncing
            - 🔧 Configurable settings
            - 📦 Multiple installation methods
            - 📖 Bilingual documentation (Persian/English)
            
            ### 📦 Installation | نصب
            
            See [INSTALLATION.md](https://github.com/${{ github.repository }}/blob/main/INSTALLATION.md) for detailed instructions.
            
            برای راهنمای کامل [INSTALLATION.md](https://github.com/${{ github.repository }}/blob/main/INSTALLATION.md) را ببینید.
            
            ### 🚀 Quick Start | شروع سریع
            
            1. Install [Tampermonkey](https://www.tampermonkey.net/)
            2. Download and install `rtl-fixer.user.js`
            3. Visit GitHub Copilot and enjoy RTL text! ✅
          files: |
            rtl-fixer.js
            rtl-fixer.user.js
          draft: false
          prerelease: false
